node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json

# local-only scratch (downloaded asset source, throwaway test scripts)
/asset-src/
/*.mts
/*.mjs

# Solana keypairs — never commit a private key
/.secrets/

# Rust/Anchor build output (cargo-build-sbf) — rebuild locally, don't commit
programs/*/target/

# Claude Code local settings
/.claude/
